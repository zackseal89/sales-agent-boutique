---
description: Add a new UI module without interfering with others.
---

-Identify required module location (/dashboard/...).

-Create isolated folder + its own components.

-Add service layer API wrapper.

-Build UI using mocked data first.

-Connect to real API via service layer once stable.

-Test responsiveness + edge cases.

-Output: files added + integration notes.

Guardrails:

-No direct fetch calls in components.

-No editing other modules unless strictly required.

4. AI Agent Prompt Update Workflow